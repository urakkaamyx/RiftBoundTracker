[project]
name = "riftkeep-rules-engine"
version = "1.0.1"
description = "Deterministic reference engine for RiftKeep rules ingestion, retrieval, proof planning, and adjudication."
requires-python = ">=3.11"
dependencies = ["PyMuPDF>=1.24"]

[tool.pytest.ini_options]
pythonpath = ["src"]
